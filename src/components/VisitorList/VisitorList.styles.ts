import styled from 'styled-components'

export const BoxList = styled.div`
  min-height: 80vh;
  margin-top: 1rem;
  padding: 0 0.5rem;
  overflow-y: auto;

  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 1.6rem;
`
